// views/Reports/BuyersByMilestone/Reports/BuyersByMilestone.spec.ts

import React from "react";
// import { render, screen } from "@testing-library/react";
import "@testing-library/jest-dom/extend-expect";

import BuyersByMilestone from "./BuyersByMilestone";

describe("<BuyersByMilestone />", () => {
  const component = BuyersByMilestone;

  it("should create", () => {
    expect(component).toBeTruthy();
  });
});
